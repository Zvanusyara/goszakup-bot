# Тестирование без токена API

Пока вы ждете токен от АО "Центр Электронных Финансов", можете протестировать весь функционал бота с моковыми данными.

## Что работает в тестовом режиме

✅ Генерация тестовых лотов с реалистичными данными
✅ Распределение лотов по менеджерам (по регионам и ключевым словам)
✅ Отправка уведомлений в Telegram
✅ Кнопки Принять/Отклонить
✅ Запрос причины отклонения
✅ Сохранение в базу данных
✅ Генерация Excel отчетов
✅ Автоматический парсинг каждую минуту

❌ Не работает: реальный парсинг с портала goszakup.gov.kz

---

## Вариант 1: Тестирование только парсера

Самый простой тест - проверка генерации данных:

```bash
./venv/bin/python test_mock_parser.py
```

**Что проверяется:**
- Генерация тестовых лотов
- Распределение по менеджерам
- Вывод статистики

**Время:** ~5 секунд

---

## Вариант 2: Полное тестирование системы

Запуск всего бота с моковыми данными:

```bash
./venv/bin/python main_test.py
```

**Что проверяется:**
- ✅ Инициализация базы данных
- ✅ Генерация тестовых лотов каждую минуту
- ✅ Отправка уведомлений менеджерам
- ✅ Отправка уведомлений админу
- ✅ Telegram бот (команды `/start`, `/stats`)
- ✅ Обработка ответов (Принять/Отклонить)
- ✅ Сохранение причины отклонения

### Пошаговая инструкция:

#### 1. Запустите тестовый бот:

```bash
./venv/bin/python main_test.py
```

Вы увидите:
```
============================================================
🧪 ТЕСТОВЫЙ РЕЖИМ: Запуск системы с моковыми данными
============================================================
📊 Инициализация базы данных...
⏰ Запуск планировщика парсинга...
🧪 ТЕСТОВЫЙ РЕЖИМ: Запуск парсинга с моковыми данными...
```

#### 2. Откройте Telegram и напишите боту `/start`

Сделайте это от имени:
- Админа (ваш ID: 388266210)
- Менеджера 1 (ID: 8280649036) - если есть доступ
- Менеджера 2 (ID: 5745700582) - если есть доступ

#### 3. Дождитесь первого парсинга

Парсинг происходит:
- Сразу при запуске
- Каждую минуту после этого

#### 4. Проверьте уведомления

Менеджеры получат сообщения с кнопками:
```
🆕 НОВОЕ ОБЪЯВЛЕНИЕ

📌 Номер: TEST-2025-XXXX
🏢 Организация: ...
📍 Регион: ...
💼 Лот: ...
🔑 Ключевое слово: ...

[✅ Принять] [❌ Отклонить]
```

#### 5. Протестируйте кнопки

**Нажмите "Принять":**
- Статус меняется на "Принято"
- Админ получает уведомление

**Нажмите "Отклонить":**
- Бот просит ввести причину
- Напишите любой текст (например: "Не подходит по условиям")
- Причина сохраняется в БД
- Админ получает уведомление с причиной

#### 6. Проверьте команду `/stats`

Напишите боту `/stats` - увидите:
```
📊 СТАТИСТИКА МЕНЕДЖЕРА

✅ Принято: X
❌ Отклонено: X
⏳ Ожидает ответа: X

📈 Всего объявлений: X
```

#### 7. Проверьте базу данных

```bash
./venv/bin/python -c "from database.crud import AnnouncementCRUD; print(f'В БД: {len(AnnouncementCRUD.get_all())} объявлений')"
```

#### 8. Сгенерируйте отчет

```bash
./venv/bin/python generate_report.py
```

Выберите:
- `1` - За всё время
- Enter - Все менеджеры

Откройте созданный Excel файл в `reports/`

#### 9. Остановите бот

Нажмите `Ctrl+C` в терминале

---

## Вариант 3: Тестирование отдельных компонентов

### Тест только базы данных:

```bash
./venv/bin/python database/models.py
```

### Тест реального парсера (с токеном):

```bash
./venv/bin/python test_parser.py
```

---

## Структура тестовых данных

### Тестовые организации:
1. **г. Алматы** - ГУ "Городская поликлиника №5"
2. **Акмолинская область** - КГП "Областная больница"
3. **г. Астана** - ТОО "Медицинский центр Астана"
4. **Туркестанская область** - КГУ "Туркестанская областная больница"
5. **Алматинская область** - ГКП "Алматинская областная клиническая больница"

### Тестовые лоты:
- Медицинские изделия для операционных
- Аренда медицинского оборудования
- Химические реагенты для лабораторий
- Запасные детали для медицинского оборудования
- Шприцы одноразовые
- Аренда помещений под медицинский кабинет

### Распределение по менеджерам:

**Менеджер 1** получит лоты из:
- г. Алматы
- Алматинская область
- г. Астана

**Менеджер 2** получит лоты из:
- Акмолинская область
- Туркестанская область

---

## Чек-лист тестирования

- [ ] Запуск тестового парсера
- [ ] Генерация тестовых лотов
- [ ] Распределение по менеджерам
- [ ] Отправка уведомлений в Telegram
- [ ] Кнопка "Принять" работает
- [ ] Кнопка "Отклонить" работает
- [ ] Запрос причины отклонения
- [ ] Сохранение в базу данных
- [ ] Команда `/start`
- [ ] Команда `/stats`
- [ ] Генерация Excel отчета
- [ ] Автоматический парсинг каждую минуту

---

## Переход на реальный API

Когда получите токен:

1. **Добавьте токен в `.env`:**
   ```env
   GOSZAKUP_API_TOKEN=ваш_токен_здесь
   ```

2. **Запустите обычную версию:**
   ```bash
   ./venv/bin/python main.py
   ```

3. **Вместо `main_test.py` используйте `main.py`**

---

## Troubleshooting

### Проблема: Уведомления не приходят

**Решение:**
1. Убедитесь, что отправили боту `/start`
2. Проверьте Telegram ID в `config.py`
3. Проверьте токен бота в `.env`

### Проблема: "Manager not found"

**Решение:**
Моковые данные генерируют только 5 регионов. Если регион не совпадает с `config.py`, менеджер не будет назначен.

### Проблема: Дубликаты не создаются

**Решение:**
Каждый тестовый лот имеет уникальный номер с временной меткой. Для теста дубликатов запустите парсинг дважды за одну минуту.

---

**Успешного тестирования! 🧪**
