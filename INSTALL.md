# Инструкция по установке и запуску

## 1. Подготовка окружения

### Установка Python
Убедитесь, что установлен Python 3.10 или выше:
```bash
python --version
```

### Создание виртуального окружения
```bash
cd goszakup_bot
python -m venv venv
```

### Активация виртуального окружения

**Linux/Mac:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

## 2. Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

## 3. Настройка конфигурации

### Отредактируйте файл `.env`:

```bash
nano .env
```

**Важно!** Укажите:

#### 1. ADMIN_TELEGRAM_ID:

```env
ADMIN_TELEGRAM_ID=YOUR_ADMIN_TELEGRAM_ID
```

**Как узнать свой Telegram ID:**
1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш ID
3. Вставьте в `.env`

#### 2. GOSZAKUP_API_TOKEN (ОБЯЗАТЕЛЬНО):

```env
GOSZAKUP_API_TOKEN=your_token_here
```

**Как получить токен API goszakup.gov.kz:**

API портала goszakup.gov.kz требует авторизационный токен. Для его получения:

1. **Отправьте письмо** в АО "Центр Электронных Финансов" со следующим текстом:

   ```
   Настоящим, просим дать доступ к унифицированным сервисам Портала
   государственных закупок и выпустить токен для авторизации.
   Данные унифицированных сервисов планируется использовать для
   мониторинга закупок и оповещения менеджеров о релевантных лотах.
   ```

2. **Контакты для запроса:**
   - Email: support@ecc.kz
   - Телефон: 8(7172) 73 55 15 или +7 705 956 53 88
   - Время работы: 7/7 с 09:00 до 21:00

3. **Срок действия:** Токен выдается на 1 год. После истечения срока можно перевыпустить самостоятельно в Профиле → "Выпуск токена (для разработчиков)"

4. **После получения токена** вставьте его в файл `.env`:
   ```env
   GOSZAKUP_API_TOKEN=73c7ada9cbc2611675ec420a44abb12b
   ```

**ВАЖНО:** Без токена парсер не сможет получать данные с портала!

## 4. Инициализация базы данных

```bash
python database/models.py
```

Вы должны увидеть:
```
✅ База данных инициализирована успешно!
📊 Созданы таблицы: announcements, manager_actions, parsing_logs
```

## 5. Тестирование

### Тест парсера (опционально):
```bash
python test_parser.py
```

Это проверит:
- Подключение к API goszakup.gov.kz
- Поиск лотов
- Распределение по менеджерам

## 6. Запуск системы

### Запуск в обычном режиме:
```bash
python main.py
```

### Запуск в фоновом режиме (Linux/Mac):
```bash
nohup python main.py > output.log 2>&1 &
```

### Остановка фонового процесса:
```bash
ps aux | grep main.py
kill [PID]
```

## 7. Проверка работы

1. Отправьте боту команду `/start`
2. Проверьте, что бот отвечает
3. Дождитесь первого парсинга (произойдет сразу после запуска)
4. Проверьте логи в директории `logs/`

## 8. Генерация отчетов

```bash
python generate_report.py
```

Следуйте инструкциям на экране для выбора периода и фильтров.

## 9. Структура проекта

```
goszakup_bot/
├── bot/                # Telegram бот
├── database/           # База данных
├── parsers/            # Парсеры API
├── reports/            # Генерация отчетов
├── utils/              # Утилиты
├── logs/              # Логи (создается автоматически)
├── config.py          # Конфигурация
├── main.py            # Запуск системы
└── .env               # Переменные окружения
```

## 10. Troubleshooting

### Проблема: Бот не отвечает
**Решение:**
1. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
2. Убедитесь, что токен правильный
3. Проверьте логи в `logs/`

### Проблема: Парсер не находит лоты
**Решение:**
1. Проверьте доступность API: `https://ows.goszakup.gov.kz/v3/lots`
2. Увеличьте `days_back` в поиске
3. Проверьте ключевые слова в `config.py`

### Проблема: Менеджер не получает уведомления
**Решение:**
1. Проверьте `telegram_id` менеджера в `config.py`
2. Убедитесь, что менеджер начал диалог с ботом (`/start`)
3. Проверьте регионы и ключевые слова

### Проблема: База данных не создается
**Решение:**
1. Проверьте права доступа к директории
2. Убедитесь, что SQLite установлен
3. Для PostgreSQL проверьте `DATABASE_URL` в `.env`

## 11. Автозапуск при загрузке системы (Linux)

### Создайте systemd service:

```bash
sudo nano /etc/systemd/system/goszakup-bot.service
```

```ini
[Unit]
Description=Goszakup Monitoring Bot
After=network.target

[Service]
Type=simple
User=YOUR_USERNAME
WorkingDirectory=/path/to/goszakup_bot
Environment="PATH=/path/to/goszakup_bot/venv/bin"
ExecStart=/path/to/goszakup_bot/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

### Активируйте service:

```bash
sudo systemctl daemon-reload
sudo systemctl enable goszakup-bot
sudo systemctl start goszakup-bot
```

### Проверка статуса:

```bash
sudo systemctl status goszakup-bot
```

## 12. Поддержка

Если возникли проблемы:
1. Проверьте логи в `logs/`
2. Запустите `python test_parser.py`
3. Проверьте конфигурацию в `config.py` и `.env`

---

**Успешной работы с системой! 🚀**
