# Настройка интеграции с Google Sheets

Это руководство поможет вам настроить автоматическую синхронизацию данных из базы данных с Google Таблицами.

## Шаг 1: Создание проекта в Google Cloud Console

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий:
   - Нажмите на выпадающий список проектов в верхней части страницы
   - Нажмите "New Project" (Новый проект)
   - Введите название проекта (например, "Goszakup Bot")
   - Нажмите "Create" (Создать)

## Шаг 2: Включение Google Sheets API

1. В Google Cloud Console перейдите в **APIs & Services** > **Library**
2. Найдите **Google Sheets API**
3. Нажмите на результат поиска
4. Нажмите кнопку **Enable** (Включить)
5. Повторите то же самое для **Google Drive API**

## Шаг 3: Создание сервисного аккаунта

1. Перейдите в **APIs & Services** > **Credentials** (Учетные данные)
2. Нажмите **Create Credentials** > **Service Account** (Создать учетные данные > Сервисный аккаунт)
3. Заполните форму:
   - **Service account name**: goszakup-bot (или любое другое имя)
   - **Service account description**: Service account for Goszakup Bot
   - Нажмите **Create and Continue**
4. На шаге "Grant this service account access to project":
   - Выберите роль **Editor** (Редактор)
   - Нажмите **Continue**
5. На шаге "Grant users access to this service account":
   - Можно пропустить, нажав **Done**

## Шаг 4: Создание ключа для сервисного аккаунта

1. На странице **Credentials** найдите созданный сервисный аккаунт
2. Нажмите на email сервисного аккаунта
3. Перейдите на вкладку **Keys** (Ключи)
4. Нажмите **Add Key** > **Create new key**
5. Выберите формат **JSON**
6. Нажмите **Create**
7. Файл JSON будет автоматически скачан на ваш компьютер

## Шаг 5: Сохранение ключа в проекте

1. Создайте директорию `credentials` в корне проекта:
   ```bash
   mkdir credentials
   ```

2. Переместите скачанный JSON файл в эту директорию и переименуйте его:
   ```bash
   mv ~/Downloads/your-project-xxxxx.json credentials/google_service_account.json
   ```

3. Добавьте директорию `credentials` в `.gitignore`, чтобы не закоммитить секретные данные:
   ```bash
   echo "credentials/" >> .gitignore
   ```

## Шаг 6: Создание Google Таблицы

1. Перейдите на [Google Sheets](https://sheets.google.com)
2. Создайте новую таблицу:
   - Нажмите **Blank** (Пустой файл)
   - Дайте таблице название (например, "Госзакупки - База данных")

3. Предоставьте доступ сервисному аккаунту:
   - Нажмите кнопку **Share** (Открыть доступ) в правом верхнем углу
   - Вставьте email сервисного аккаунта (он выглядит как `your-service-account@your-project.iam.gserviceaccount.com`)
   - Email можно найти в скачанном JSON файле в поле `client_email`
   - Выберите роль **Editor** (Редактор)
   - Снимите галочку "Notify people" (Уведомить людей)
   - Нажмите **Share**

4. Скопируйте ID таблицы из URL:
   - URL выглядит так: `https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit`
   - Скопируйте часть `SPREADSHEET_ID`

## Шаг 7: Настройка переменных окружения

Откройте файл `.env` в корне проекта и обновите следующие параметры:

```env
# Google Sheets Integration
GOOGLE_SHEETS_ENABLED=True
GOOGLE_SERVICE_ACCOUNT_FILE=credentials/google_service_account.json
GOOGLE_SPREADSHEET_ID=your_actual_spreadsheet_id_here
GOOGLE_SHEET_NAME=Объявления
```

Где:
- `GOOGLE_SHEETS_ENABLED=True` - включает интеграцию
- `GOOGLE_SERVICE_ACCOUNT_FILE` - путь к JSON файлу с ключами (не меняйте, если следовали инструкции)
- `GOOGLE_SPREADSHEET_ID` - ID таблицы, скопированный на шаге 6
- `GOOGLE_SHEET_NAME` - название листа в таблице (по умолчанию "Объявления")

## Шаг 8: Проверка настройки

1. Убедитесь, что все зависимости установлены:
   ```bash
   pip install -r requirements.txt
   ```

2. Запустите бота:
   ```bash
   python main.py
   ```

3. При первом запуске модуль автоматически:
   - Создаст лист "Объявления" в вашей таблице (если его нет)
   - Добавит заголовки столбцов
   - Применит форматирование

4. Проверьте лог запуска на наличие сообщения:
   ```
   ✅ Google Sheets успешно инициализирован
   ```

## Структура данных в Google Sheets

После настройки в таблице будут следующие столбцы:

| Столбец | Описание |
|---------|----------|
| ID | Внутренний ID объявления |
| Дата создания | Когда объявление было добавлено в систему |
| Номер объявления | Номер объявления на портале госзакупок |
| Ссылка | URL объявления |
| Организация | Название организации-заказчика |
| БИН | БИН организации |
| Юридический адрес | Адрес организации |
| Регион | Регион организации |
| Название лота | Название лота/тендера |
| Описание лота | Описание лота |
| Ключевое слово | По какому ключевому слову найдено |
| ID менеджера | Внутренний ID менеджера |
| Менеджер | Имя менеджера |
| Статус | Статус обработки (Ожидает/Принято/Отклонено) |
| Причина отказа | Причина отклонения (если есть) |
| Дата обновления | Когда запись была последний раз обновлена |
| Дата ответа | Когда менеджер дал ответ |
| Уведомление отправлено | Было ли отправлено уведомление |
| Админ уведомлен | Был ли уведомлен админ |

## Автоматическая синхронизация

Система автоматически синхронизирует данные с Google Sheets в следующих случаях:

1. **При добавлении нового объявления** - создается новая строка в таблице
2. **При изменении статуса** - обновляется существующая строка

### Цветовое кодирование

Статусы автоматически подсвечиваются цветом:
- 🟢 **Зеленый** - Принято (accepted)
- 🔴 **Красный** - Отклонено (rejected)
- 🟡 **Желтый** - Ожидает ответа (pending)

## Устранение проблем

### Ошибка: "Файл учетных данных Google не найден"

**Решение**: Убедитесь, что:
- Файл `google_service_account.json` находится в директории `credentials/`
- Путь в `.env` указан правильно

### Ошибка: "Permission denied" или "Access denied"

**Решение**:
- Убедитесь, что вы предоставили доступ сервисному аккаунту к таблице (см. Шаг 6)
- Проверьте, что email сервисного аккаунта указан правильно
- Убедитесь, что роль **Editor** выбрана

### Ошибка: "Spreadsheet not found"

**Решение**:
- Проверьте правильность `GOOGLE_SPREADSHEET_ID` в файле `.env`
- Убедитесь, что таблица существует и доступна

### Данные не синхронизируются

**Решение**:
1. Проверьте, что `GOOGLE_SHEETS_ENABLED=True` в `.env`
2. Перезапустите бота
3. Проверьте логи на наличие ошибок
4. Убедитесь, что API включены в Google Cloud Console

## Ручная синхронизация

Если нужно синхронизировать все существующие данные из БД:

```python
from database.crud import AnnouncementCRUD
from utils.google_sheets import get_sheets_manager

# Получить все объявления
announcements = AnnouncementCRUD.get_all_for_report()

# Синхронизировать
sheets_manager = get_sheets_manager()
stats = sheets_manager.sync_all_announcements(announcements)

print(f"Добавлено: {stats['added']}")
print(f"Обновлено: {stats['updated']}")
print(f"Ошибок: {stats['errors']}")
```

## Безопасность

**ВАЖНО**:
- Никогда не коммитьте файл `google_service_account.json` в Git
- Добавьте `credentials/` в `.gitignore`
- Не делитесь файлом с ключами
- Если ключ скомпрометирован, удалите его в Google Cloud Console и создайте новый

## Полезные ссылки

- [Google Cloud Console](https://console.cloud.google.com/)
- [Google Sheets API Documentation](https://developers.google.com/sheets/api)
- [gspread Documentation](https://docs.gspread.org/)
- [Service Accounts Documentation](https://cloud.google.com/iam/docs/service-accounts)
